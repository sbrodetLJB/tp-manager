{% extends 'base.html.twig' %}

{% block title %}Nouvelle classe — TP Manager{% endblock %}

{% block body %}
    <h1>Nouvelle classe</h1>
    {{ form_start(form) }}
        <div class="row">{{ form_row(form.nom) }}</div>
        <div class="row">{{ form_row(form.anneeScolaire) }}</div>
        <button type="submit">Créer</button>
    {{ form_end(form) }}
{% endblock %}
